{
    "name": "etch-a-sketch-web",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "description": "Browser SPA for the Etch-a-Sketch Drawing Machine. Hosts image/text/freehand input, path planning, and the WebSocket client to the controller.",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "test": "vitest run",
        "test:watch": "vitest",
        "e2e": "playwright test",
        "lint": "tsc --noEmit"
    },
    "dependencies": {
        "@preact/signals": "^1.3.0",
        "preact": "^10.24.3"
    },
    "devDependencies": {
        "@playwright/test": "^1.61.1",
        "@preact/preset-vite": "^2.9.1",
        "@types/node": "^22.9.0",
        "fast-check": "^3.22.0",
        "jsdom": "^25.0.1",
        "typescript": "^5.6.3",
        "vite": "^5.4.10",
        "vite-plugin-singlefile": "^2.0.3",
        "vitest": "^2.1.4"
    }
}
